---
- name: Update SSH Port in sshd_config 
  vars:
    newport: 10022
  ansible.builtin.lineinfile:
        path: /etc/ssh/sshd_config
        regexp: '^#?Port'
        line: 'Port {{newport}}'
        backup: yes
      notify:
        - Restart SSH

